<?xml version="1.0" encoding="utf-8"?>
    <ScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="@dimen/space_24"
            tools:context=".view.ContactFormActivity">

            <TextView
                style="@style/AppTheme.FormHeader"
                android:text="@string/form_title" />

            <TextView
                style="@style/AppTheme.FormSubheader"
                android:layout_marginTop="@dimen/space_12"
                android:layout_marginBottom="@dimen/space_12"
                android:text="@string/form_subtitle" />

            <!-- TODO 08: Include a togglegroup to select the type of contact we are creating -->
            <!-- It's recommended to use materialButtonOutlinedStyle attribute for all child buttons -->
            <!-- Any MaterialButtons added to this view group are automatically marked as checkable -->
            <!-- By default multiple buttons within the same group can be checked -->
            <!-- TODO 09: Enforce only one button checked at a time -->
            <com.google.android.material.button.MaterialButtonToggleGroup
                android:id="@+id/contactTypeToggleGroup"
                android:layout_gravity="center_horizontal"
                app:singleSelection="true"
                android:layout_marginTop="@dimen/space_12"
                android:layout_marginBottom="@dimen/space_24"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">
                <com.google.android.material.button.MaterialButton
                    android:text="@string/family_option"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"/>
                <com.google.android.material.button.MaterialButton
                    android:text="@string/friend_option"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"/>
                <com.google.android.material.button.MaterialButton
                    android:text="@string/work_option"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"/>
            </com.google.android.material.button.MaterialButtonToggleGroup>


            <com.google.android.material.textfield.TextInputLayout
                style="@style/AppTheme.TextField"
                app:startIconDrawable="@drawable/ic_perm_identity_black_24dp"
                app:helperText="@string/name_field_helper"
                app:endIconMode="clear_text">
                <com.google.android.material.textfield.TextInputEditText
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/name_field_hint" />
            </com.google.android.material.textfield.TextInputLayout>

            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/passwordTextInput"
                style="@style/AppTheme.TextField"
                app:helperText="@string/password_field_helper"
                app:counterEnabled="true"
                app:counterMaxLength="8"
                app:endIconMode="password_toggle">
                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/passwordEditText"
                    android:layout_width="match_parent"
                    android:inputType="textPassword"
                    android:layout_height="wrap_content"
                    android:hint="@string/password_field_hint" />
            </com.google.android.material.textfield.TextInputLayout>

            <!-- TODO 01: We add a MaterialButton -->
            <!-- TODO 02: Let's check the several styles available in the Material library for buttons -->
            <!-- TODO 03: We define a new style for the button -->
            <!-- TODO 05: We add an icon to the button -->
            <!-- TODO 06: We have to position the icon: gravity, padding -->
            <!-- TODO 07: We can modify the style of the button -->
            <com.google.android.material.button.MaterialButton
                android:layout_gravity="end"
                style="@style/AppTheme.Button"
                app:icon="@drawable/ic_add_a_photo_black_24dp"
                app:iconGravity="start"
                app:iconPadding="@dimen/space_12"
                android:padding="@dimen/space_8"
                android:text="@string/add_photo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>
    </ScrollView>